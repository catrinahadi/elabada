const express = require("express");
const router = express.Router();

// TODO: Implement auth routes (login, register)
// router.post("/login", loginController);
// router.post("/register", registerController);

module.exports = router;

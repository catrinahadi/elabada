const express = require("express");
const router = express.Router();

// TODO: Implement review routes (post review, get reviews for shop)
// router.post("/", postReview);
// router.get("/:shopId", getReviewsForShop);

module.exports = router;
